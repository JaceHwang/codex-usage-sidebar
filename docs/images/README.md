# Documentation image scope

Current GUI illustrations are the [native control renders](current/README.md). `placement.svg` is
an updated algorithm schematic, not a live window capture. `architecture.svg` is a conceptual data
flow illustration. Older version-named PNGs and `hero.svg` remain historical assets for old designs;
they are not current GUI evidence and are no longer used as the current README gallery.
