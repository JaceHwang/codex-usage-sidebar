# Codex Usage Sidebar macOS v0.3.6

> Release candidate — not published yet.

This macOS 14+ Apple Silicon maintenance release fixes cold-start quota-card layout corruption after
reboot and makes the footer resize guidance appear on hover before dragging. It does not change the
Windows release, which remains at v0.3.3.

## Planned assets

- Installer: `codex-usage-sidebar-macos-arm64-v0.3.6.dmg`
- Checksums: `MACOS-V036-SHA256SUMS.txt`
- Provenance: `MACOS-V036-PROVENANCE.json`

The asset can be published only after the `macos-v0.3.6` tag identifies the same verified commit
embedded in the installer payload and recorded in provenance.
