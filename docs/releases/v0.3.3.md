# Codex Usage Sidebar v0.3.3

The formal Windows 11 AMD64/x64 release is available from the [v0.3.3 GitHub Release](https://github.com/JaceHwang/codex-usage-sidebar/releases/tag/v0.3.3).
Windows ARM64 is not supported.

## Windows assets

- Installer: `codex-usage-sidebar-v0.3.3-windows-x64-setup.exe`
- Checksums: `WINDOWS-V033-SHA256SUMS.txt`
- Provenance: `WINDOWS-V033-PROVENANCE.json`
- Signed compatibility pack: `codex-usage-sidebar-v0.3.3-windows-compatibility-pack.zip`

Verify the installer before running it. The setup is intentionally unsigned and Windows may show
**Unknown publisher** after the checksum matches; do not disable Defender, SmartScreen, antivirus,
or system policy.

Installer SHA-256:

```text
5b04ef785c4e16715146986f5b293694029dc3ec8cf72a32e84bc16c1636ed08
```

Compatibility pack SHA-256:

```text
5610ff247e7d4d9cd1409c460855a3016a76d2a0221351b235f38ba7fe44f9e6
```

## Compatibility and safety

The installer embeds the schema-v2 selector catalog and a P-256 public key. It checks the HTTPS
compatibility pack before accepting a remote selector catalog; an unavailable or invalid update
keeps the packaged or previously validated catalog. Users do not edit `selectors.json` or perform
manual Codex-version adaptation.

The selector no longer rejects a Codex build solely because its file version changed. It still
requires a safe semantic UI Automation titlebar structure and hides the overlay when the structure
cannot be proven safe instead of guessing coordinates.

## Validation and provenance

- Tested source commit: `dfce4e6a93d7442e267868d8716cf882791f6155`
- Evidence-only packaging commit: `8a97e9131c36db0f6f7cf6f815235d2edae9eac3`
- Windows 11 x64 real-device evidence: 85/85 cases passed
- Release setup: current-user installation, no administrator privileges required
- Official Codex runtime: `rust-v0.147.0`, x64 Windows build

See [installation and operations](../INSTALL.md), [agent installation](../INSTALL_FOR_AGENTS.md),
and [troubleshooting](../TROUBLESHOOTING.md) for installation, repair, status, and recovery steps.
