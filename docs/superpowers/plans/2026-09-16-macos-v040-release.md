# macOS 0.4.0 release plan

1. Preserve local work; integrate current UI and collision fixes without ordinary version edits.
2. Add actual companion screenshot to bilingual README; retain clearly labelled fixture galleries.
3. Adapt the verified v0.3.5 installer chain for v0.4.0, wire exact-tag staged macOS publication and test provenance rejection.
4. Run governance checks; commit source, rebuild tracked companion from source commit, commit payload, push and merge feature PR only after checks.
5. Review generated Release Please 0.4.0 PR, rebuild changed bundle signature/provenance, run checks and merge.
6. Dispatch macOS publication from generated v0.4.0, verify uploaded immutable assets and provenance.
7. Update public download/status docs and catalog to the actually published macOS version, preserve Windows v0.3.3 status, verify and merge.
